/*
 * This file was generated by the Gradle 'init' task.
 */

pom {
    dependencyManagement {
        dependencies {
            dependency "${group}:framework-model:${version}"
            dependency "${group}:framework-web:${version}"
            dependency "${group}:framework-rule-engine:${version}"
            dependency "${group}:framework-util-common:${version}"
            dependency "${group}:framework-util-threadpool:${version}"
            dependency "${group}:framework-util-notify:${version}"
            dependency "${group}:framework-util-oss:${version}"
            dependency "${group}:framework-util-kv:${version}"
            dependency "${group}:framework-integrate-swagger:${version}"
            dependency "${group}:framework-integrate-rocketmq:${version}"
            dependency "${group}:framework-integrate-redis:${version}"
            dependency "${group}:framework-integrate-job:${version}"
            dependency "${group}:framework-dao-mybatis:${version}"
            dependency "${group}:framework-dao-mybatis-plus:${version}"
        }
    }
}


